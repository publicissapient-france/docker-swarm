---
- hosts: all
  remote_user: ubuntu
  sudo: yes
  tasks:
  - name: Set up authorized_keys for the admin user
    authorized_key: user=ubuntu
                    key="{{ item }}"
    with_file:
    - public_keys/docker-temp.pub
